#!/usr/local/bin/zsh -f

if [[ "$1" == "" ]]; then
    echo 'Usage: $0 <open_id>'
    exit 0;
fi
source ~/.aliases;
CAT="$(wx "cat ~/src/werun/log/access_token.json"|awk  -F'"' '{print $4}')"

#curl https://api.weixin.qq.com/cgi-bin/userinfo?access
echo $CAT;
