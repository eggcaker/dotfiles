#!/usr/bin/env node


var hex = process.argv[2];

if (hex.length == 6 ) {
  console.log(parseInt(hex.slice(0,2), 16));
  console.log(parseInt(hex.slice(2,4), 16));
  console.log(parseInt(hex.slice(4,6), 16));
} else if (hex.length == 2) {
  console.log(parseInt(hex, 16));
}

