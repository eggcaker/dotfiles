#!/usr/bin/env zsh

P=${1};
place=${P:=Food}

echo "`date '+%Y-%m-%d'` * \"${1}\"" >> $LEDGER_FILE
echo "  Expenses:Routine:${place}  ${2}" >> $LEDGER_FILE
echo "  Assets:$3" >> $LEDGER_FILE

echo "记帐成功!"
