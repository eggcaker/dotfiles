#!/usr/bin/env perl 

use Modern::Perl;
use Mojo::UserAgent;
use utf8;
use Encode 'encode';



my $ua = Mojo::UserAgent->new;

my $result = encode('UTF-8', $ua->get('http://www.baidu.com/s?wd=site%3A150019.com')->res->dom
    ->find('.site_tip > strong')->[0]->text);

say $result;
